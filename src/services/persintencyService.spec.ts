describe('TESTE 1...', () => {
  it('should return one', () => {
    const number = 1;
    expect(number).toBe(1);
  });
});

describe('TESTE 2...', () => {
  test('should return Leo', () => {
    const name = 'Leo';
    expect(name).toBe('Leo');
  });
});
